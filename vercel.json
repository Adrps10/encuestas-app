{
  "buildCommand": "cd frontend && npm install --legacy-peer-deps && npm run build:prod",
  "outputDirectory": "frontend/dist/encuestas-app",
  "installCommand": "npm install --legacy-peer-deps",
  "framework": "angular",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
