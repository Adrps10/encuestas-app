{
  "buildCommand": "cd frontend && npm ci --legacy-peer-deps && npm run build:prod",
  "outputDirectory": "frontend/dist/encuestas-app",
  "installCommand": "cd frontend && npm ci --legacy-peer-deps",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
